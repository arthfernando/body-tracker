<h1><%= @user.name %></h1>

<p><%= @user.birthdate %></p>
<p><%= @user.email_address %></p>

<%= link_to "Back", users_path %>
<%= link_to "Edit", edit_user_path(@user) %>
<%= button_to "Delete", @user, method: :delete, data: { turbo_confirm: "Are you sure you want to delete this user?"} %>